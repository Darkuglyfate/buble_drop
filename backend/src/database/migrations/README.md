TypeORM migrations for BubbleDrop backend.

Use npm scripts from `backend/package.json`:

- `npm run db:migration:create`
- `npm run db:migration:generate`
- `npm run db:migration:run`
- `npm run db:migration:revert`
